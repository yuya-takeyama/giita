.row
  .col-md-12
    .article
      .article-subject
        h1
          = @issue.title
      .article-body
        div
          == @issue.parsed_body
    .comments
      - @comments.each do |comment|
        .comment
          .comment-container
            .comment-avatar
              img src=comment.user.avatar_url width=40 heigh=40

            .comment-header
              .comment-author
                == link_to_user comment.user
              .comment-time.timeago title=comment.created_at.iso8601
                = comment.created_at
            .comment-body
              == comment.parsed_body
